ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.travel.ApplicationKt.module ]
    }
}

database {
    driver = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://localhost:3306/travel_db"
    user = "user"
    password = "password"
}

jwt {
    secret = ${JWT_SECRET}
    issuer = "com.travel"
    audience = "users"
    realm = "ktor sample app"
}
