ALTER TABLE destinations
    ADD COLUMN slug VARCHAR(255) UNIQUE,
    ADD COLUMN address VARCHAR(512),
    ADD COLUMN city VARCHAR(255),
    ADD COLUMN tags TEXT,
    ADD COLUMN best_time_to_visit VARCHAR(255),
    ADD COLUMN opening_hours VARCHAR(255),
    ADD COLUMN price_from DECIMAL(10,2),
    ADD COLUMN price_to DECIMAL(10,2),
    ADD COLUMN external_links TEXT,
    ADD COLUMN address_link VARCHAR(1024),
    ADD COLUMN avg_rating DOUBLE DEFAULT 0.0,
    ADD COLUMN review_count INT DEFAULT 0,
    ADD COLUMN views_count BIGINT DEFAULT 0,
    ADD COLUMN favorites_count INT DEFAULT 0,
    ADD COLUMN status ENUM('ACTIVE', 'INACTIVE', 'DRAFT') DEFAULT 'ACTIVE',
    ADD COLUMN sort_order INT DEFAULT 0,
    ADD COLUMN created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    ADD COLUMN updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
